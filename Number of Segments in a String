class Solution {
    public int countSegments(String s) {
        String[] arr = s.split(" ");
        int count =0;
        for(String ss:arr){
            if(ss.equals("")){
                count++;
            }
        }
        return arr.length-count;
    }
}
